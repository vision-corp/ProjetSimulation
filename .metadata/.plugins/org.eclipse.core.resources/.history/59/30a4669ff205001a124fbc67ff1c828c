/**
 * Binomiale.java						13 nov. 2019
 * IUT info1 2018-2019 TD2, no copyright, no copyleft
 */
package projetModel;

/**
 * Classe de calcul de la loi Binomiale
 * @author florian hyver
 *
 */
public class Binomiale implements loi {
    
    /** paramètre n de la loi binomiale appartenant a N* */
    private int n;
    
    
    /** paramètre p de la loi binomiale et Bernouilli appartenant a [0,1] */
    private double p;
    

    /**
     * simmule le fonctionnement de la loi binomiale
     * @return le resultat de la loi binomiale
     */
    public double simuler() {
        return 0;
        
    }
    
    /**
     * simule le fonctionnement de la loi de Bernouilli
     * @param X 
     * @return le resultat de la loi de bernouilli
     */
    public double simulerBernouilli() {
        
    }
}
